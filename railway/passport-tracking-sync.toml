[build]
builder = "NIXPACKS"

[deploy]
# Chromium runtime libs are installed at build time by the root nixpacks.toml
# (2026-09-08), so nothing hits an Ubuntu mirror at boot.
startCommand = "node customer-service/sync/syncPassportDelivery.js --limit 50"
cronSchedule = "37 * * * *"
