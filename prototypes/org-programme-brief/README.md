# Virtual Closet in brief

The one-screen version of the programme page: the pitch, three doors, four terms, contact. Built from `template.html` by replacing `{{IMG_JSON}}` with the logo data URI. Full terms live in `../org-programme-page/`.
